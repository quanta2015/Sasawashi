// import React,{useState,useEffect} from 'react';// import { inject,observer,MobXProviderContext } from 'mobx-react'// import classnames from 'classnames';// import {API_SERVER} from '@/constant/apis'// import { GUIDE_LIST} from '@/constant/data'// import s from './index.module.less';// import TitleLine from '@/component/TitleLine'// import right from '@/img/right.svg'// const Guide = () => {//   // const [detail,setDetail] = useState(false)//   const [list,setList]= useState(GUIDE_LIST)//   const doShowDetail =(o,i)=>{//     list[o].list[i].show = !list[o].list[i].show //     setList([...list])//     console.log(list)//   }// const RenderGuideTitle = (list)=>(//   <>//   {list.map((item,i)=>//     <React.Fragment key={i}>    //       <TitleLine title={item.name} type={'all'} />  //       {/*<h1>{item.name}</h1>      */}     //       {item.ret.map((o,j) =>//         <div key={j} className={classnames(s.wrap,{'show': o.show})}>//           <div className={s.title} onClick={()=>doShowDetail(i,j)}>//             <span>{o.title}</span>//             <img src={right} /> //           </div>//             <p className={s.detail}>//               {o.detail.map((p,k)=><i key={k}>{k+1}. {p}</i>)}//             </p>  //         </div>//         )}       //     </React.Fragment>   //     )//   }//   </>//   )//   return (//     <section>//       {//       <div className={s.guideList}>//         <div className={s.header}>//           <div className={s.main}>//             <div className={s.title}>//               <h1>ご利用ガイド</h1>//             </div>//             <div className={s.content}>//               {RenderGuideTitle(GUIDE_LIST)}//             </div> //           </div>//         </div>//       </div>}//     </section>//   )// }// export default  inject('store')(observer(Guide))import React, {useState,useEffect} from 'react';import { inject,observer,MobXProviderContext } from 'mobx-react'import classnames from 'classnames';import {API_SERVER} from '@/constant/apis'import { GUIDE_LIST} from '@/constant/data'import s from './index.module.less';import TitleLine from '@/component/TitleLine'import ToTop     from '@/component/ToTop'import right from '@/img/right.svg'const Guide = () => {  const [list,setList] = useState(GUIDE_LIST)  const doShowDetail =(i,j)=>{    console.log(i,j)    list[i].list[j].show = !list[i].list[j].show    setList([...list])    console.log(list)  }  return (    <section>      <div className={s.guide}>        <div className={s.header}>          <div className={s.main}>            <div className={s.title}>              <h1>ご利用ガイド</h1>            </div>            <div className={s.list}>              {list.map((item,i)=>                <React.Fragment key={i}>                  <TitleLine title={item.name}  type={'all'} />                  {item.list.map((o,j)=>                    <div key={j} className={classnames(s.wrap,{'show':o.show})}>                      <div className={s.title} onClick={()=>doShowDetail(i,j)}>                        <span>{o.title}</span>                        <img src={right} />                      </div>                      <p className={s.detail}>                        {o.detail.map((p,k)=> <i key={k}>{k+1}. {p}</i>)}                      </p>                    </div>                  )}                </React.Fragment>              )}            </div>          </div>        </div>        <ToTop />      </div>    </section>  )}export default  inject('store')(observer(Guide))