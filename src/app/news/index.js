import React,{useState,useEffect} from 'react';import { inject,observer,MobXProviderContext } from 'mobx-react'import classnames from 'classnames';import {API_SERVER} from '@/constant/apis'import { NEWS_LIST} from '@/constant/data'import s from './index.module.less';import TitleLine from '@/component/TitleLine'const News = () => {  const [detail,setDetail] = useState(false)  const RenderNewsList = (list)=>(    <>    {list.map((item,i)=>       <div key={i} className={s.detail} onClick={()=>setDetail(!detail)}>        <h1>{item.date}</h1>        <p>{item.title}</p>      </div>    )}    </>  )    return (    <section>      {!detail &&      <div className={s.newsList}>        <div className={s.header}>          <div className={s.main}>            <div className={s.title}>              <h1>News</h1>              <span>お知らせ</span>            </div>            <div className={s.content}>              {RenderNewsList(NEWS_LIST)}              <div className={s.pager}>                <span>1</span>              </div>             </div>           </div>        </div>      </div>}      {detail &&      <div className={s.newsDetail}>        <div className={s.date}>2023.01.30</div>        <TitleLine title={'新闻标题'} type={'all'}/>        <p className={s.detail}>          {"1月31日(月)20:00～24：00の間、\nサイト改装のためSASAWASHI ONLINE SHOPを一時的にクローズさせていただきます。\n\nご不便をおかけしますが、何卒ご理解いただきますようお願い申し上げます。"}        </p>        <div className={s.return} onClick={()=>setDetail(!detail)}>News 一覧に戻る</div>      </div>}    </section>  )}export default  inject('store')(observer(News))