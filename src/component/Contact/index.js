import React,{useState,useEffect} from 'react';import classnames from 'classnames';import {API_SERVER} from '@/constant/apis'import {CONTACT_DATA, BASE} from '@/constant/data'import s from './index.module.less';const Contact = ({}) => {  return (    <section className={s.gc}>      <div className={s.contact}>                <div className={s.left}>          <h1>Contact</h1>          <span>お問い合わせ</span>          <div className={s.p}>            <span>商品に関するご意見やご質問など、</span>            <span>どうぞお気軽にお問い合わせください。</span>          </div>        </div>        <div className={s.right}>          {CONTACT_DATA.map((item,i)=>            <div key={i} className={s.col}>              <label>{item.title}</label>              {item.list.map((o,j)=>                <a key={j} href={`${BASE}${o.url}`} target="_blank">{o.k}</a>              )}            </div>          )}        </div>      </div>    </section>  )}export default Contact