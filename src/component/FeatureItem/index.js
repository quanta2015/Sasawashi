import React,{useState,useEffect} from 'react';import classnames from 'classnames';import s from './index.module.less';const FeatureItem = ({item,index,left=true}) => {  return (    <div key={index} className={classnames(s.featureItem,{right:!left})}>      <div className={s.img}>        <picture>          <source media="(min-width: 769px)" srcSet={item.img} />          <img src={`${item.img.split('.jpg')[0]}_sp.jpg`} />        </picture>      </div>      <div className={s.detail}>        <p>{item.en}          <span>{(index+1).toString().padStart(2,'0')}</span>        </p>        <h3>{item.title}</h3>        <label className={s.desc}>{item.content}</label>      </div>    </div>  )}export default  FeatureItem